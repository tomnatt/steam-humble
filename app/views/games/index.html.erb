<div class="row">
  <div class="col">
    <h1><%= if params[:view_param] == 'failed-db'
        "Failed Games"
      else
        "All Games"
      end %></h1>
    <p class="lead">Manually <%= link_to 'add game', new_game_path, id: 'add-game' %></p>
    <p class="lead">Auto-update <%= link_to 'database', update_db_games_path, id: 'update-db' %></p>
    <p class="lead">Destroy all in <%= link_to 'database', destroy_db_games_path, data: { confirm: 'Are you sure?' }, id: 'destroy-db' %></p>
    <p class="lead">Show database: <%= link_to 'Full', games_path, id: 'full-db' %> |
      <%= link_to 'Failed', games_path(view_param: 'failed-db'), id: 'failed-db' %></p>
    <p class="lead">Number of games: <%= @games.count %></p>
  </div>
</div>

<div class="row">
  <table class="col table">
    <thead>
      <tr>
        <th>Game name</th>
        <th>Month</th>
        <th>Steam ID</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @games.each do |game| %>
        <tr>
          <td><%= game.game_name %></td>
          <td><%= game.humble_bundle %></td>
          <td><%= game.steam_appid %></td>
          <td><%= link_to 'Show', game %></td>
          <td><%= link_to 'Edit', edit_game_path(game) %></td>
          <td><%= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
